# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty/skeleton repository. It contains a single placeholder file (`test`) with no application code, no dependencies, no build system, and no services.

- **No package manager or dependency files** exist (no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, etc.).
- **No lint, test, or build commands** are configured.
- **No services** need to be started.
- **No Docker or docker-compose** configuration exists.

When application code is added to this repository, this file should be updated with relevant setup, build, test, and run instructions.
